import 'package:flutter/material.dart';

class Directory extends StatelessWidget {
  const Directory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Diectorio"),
    );
  }
}
